# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# Never commit .env — it's already in .gitignore.

# ESPHome API encryption key from your device YAML (api.encryption.key)
ENCRYPTION_KEY=
